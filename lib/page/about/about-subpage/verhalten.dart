import 'package:app/app.dart';
class VerhaltenPage extends  StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Verhalten auf Demos'),
      ),
      body: Center(
        child: Text(
          'Hier kommt unser Verhalten auf Demos hin',
        ),
      ),
    );
  }
}
